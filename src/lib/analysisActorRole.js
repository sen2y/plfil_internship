export function analysisActorRole(list) {

    let returnList = {};
    for (let i = 0; i < list.length; i++) {
        const a = list[i]
        console.log(a.role)
        //returnList = { countRole: countRole, countCate: countCate };
    }
    return returnList;
}